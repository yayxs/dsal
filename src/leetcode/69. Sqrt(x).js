/**
 * @desc 给你一个非负整数 x ，计算并返回 x 的 算术平方根 。
 * @param {number} x
 * @return {number}
 */
var mySqrt = function(x) {
 if(x ===0 || x ===1){
   return x
 }
let left = 0
let right =x

};

console.log(mySqrt(4))
