
/**
 * 数组有序 不存在重复
 * @param {*} sortedArr
 * @param {*} target
 */
function binaryFind(sortedArr,target){
  let start = 0,end = sortedArr.length - 1

  while(start +1<end){ // 0 < 1
    let mid = start + (end-start) /2 // 0+(1)
    // if(){
    //   start = mid
    // }else{
    //   end = mid
    // }
  }
}


binaryFind([2,2],3)
